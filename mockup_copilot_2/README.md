# Throughline / Mockup 02

A second visual mockup for the Throughline playtext scene visualizer. This version uses a dark, structural layout with ruled coordinates and restrained line-drawing motion.

Open `index.html` directly or serve this folder with a static web server.
